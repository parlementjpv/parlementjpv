[build]
  functions = "netlify/functions"
  publish = "."

[[redirects]]
  from = "/admin/*"
  to = "/admin/bureau.html"
  status = 200

# (Optionnel) Bloque l'accès direct à /admin si pas loggé via Identity.
# La vraie protection se fait côté JS + Functions (JWT), mais ça évite les erreurs.
